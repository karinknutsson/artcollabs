<div class="nav-artcollabs container-fluid center-flex">

    <% if user_signed_in? %>
        <div class="row fill">
            <div class="col-4 col-md-7 no-padding"></div>
            <div class="col-6 col-md-4 end-flex no-padding">

                <% unless current_page?(root_path) %>
                    <%= render "shared/navbar_search_form" %>
                <% end %>
            </div>
            <div class="col-2 col-md-1 center-flex no-padding">

               <div class="dropdown dropleft navbar-dropdown center-flex">
                    <button type="button" class="btn-menu" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fas fa-solid fa-bars"></i>
                    </button>
                    <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                        <%= link_to "Dashboard", dashboard_path, class: "dropdown-item" %>
                        <%= link_to "New project", new_project_path, class: "dropdown-item" %>
                        <%= link_to "Profile", profile_path(current_user), class: "dropdown-item" %>
                        <%= link_to "Settings", edit_user_registration_path, class: "dropdown-item" %>
                        <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
                    </div>
                </div>

            </div>
        </div>
   <% else %>

            <div class="nav-btn-login">
                <%= link_to "Log in", new_user_session_path, class: "white btn-artcollabs white margin-right-32"%>
                <%= link_to "Sign up", new_user_registration_path, class: "white btn-artcollabs white"%>
            </div>

    <% end %>

</div>

<div class="nav-logo-container">
    <%= render 'shared/logo' %>
</div>
