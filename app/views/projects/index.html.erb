<% @projects.each do |project| %>
  <br><%= link_to "#{project.title}", project_path(project)  %>
  <!-- favourite_path with project it -->
  <!-- form with icon that calls new path (on submit) -->
  
  <!-- if current_user -  if @user_favourites.project.includes(project) then display -->
<% end %>